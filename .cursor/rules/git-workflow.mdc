---
description: Git 分支与提交纪律（全局）
alwaysApply: true
---

# Git 分支与提交纪律

## 分支

- **禁止**在 `main` / `master` 上直接修改代码、spec 或配置。
- 所有改动必须在任务分支上进行，命名格式：`<prefix>/<任务名>`。
- 允许的前缀：`feature/`、`bugfix/`、`update/`、`refactor/`、`docs/`。

示例：`feature/navigation-pages`、`bugfix/cta-link`、`docs/git-governance-spec`

开工前检查当前分支；若在 `main`，先创建任务分支再改。

## 提交

- **未经用户明确说「可以提交」，不得执行 `git commit`、`git push` 或等价的版本提交操作。**
- 可先改文件、可创建分支；提交与推送须等用户授权。
- 用户授权后，commit message 用 `type(scope): 描述` 风格，写清「为什么改」。

## 参考

- demo 项目细则：`demo/spec/governance/GIT_GOVERNANCE_SPEC.md`
- demo agent 规则：`demo/AGENTS.md`
